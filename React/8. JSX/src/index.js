import React from "react";
import ReactDOM from "react-dom";
import Heading from "./Heading";
import List from "./List"; // Import the corrected List component

ReactDOM.render(
  <div>
    <Heading />
    <List />
  </div>,
  document.getElementById("root")
);
